{
  "name": "@manaflair/redux-batch",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "babel -d build/ sources/",
    "prepare": "yarn run build"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0"
  },
  "peerDependencies": {
    "redux": "*"
  }
}
